#include <bits/stdc++.h>
#define fore (i,a,b) for (long i=a; i<=b; ++i)

using namespace std;

long long a,b;

int main()
{
    ios_base::sync_with_stdio(0);
    freopen("PDIGIT.inp","r",stdin);
    freopen("PDIGIT.out","w",stout);
    cin >> a >> b;
}
