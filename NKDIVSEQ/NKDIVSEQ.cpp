#include <bits/stdc++.h>

using namespace std;

long long n,a[100007];

int main()
{
    ios_base::sync_with_stdio(0);
    freopen("NKDIVSEQ.inp","r",stdin);
    freopen("NKDIVSEQ.out","w",stdout);
    cin >> n;
    long d=0;
    for (long i=1; i<=1000; i++)
    {
        for (long j=1; j<=i; j++)
            a[++d]=
    }
}
